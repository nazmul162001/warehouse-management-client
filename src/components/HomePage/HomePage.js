import React from 'react';
import Banner from '../Banner/Banner';

const HomePage = () => {
  return (
    <section className=''>
      <Banner></Banner>
    </section>
  );
};

export default HomePage;